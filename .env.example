# Supabase — the wearable's live link and history store.
#
# The wearable inserts rows into `telemetry_readings` and `fall_events`;
# the browser subscribes via Supabase Realtime and reads the same tables.
# Use the project's *publishable* key (browser-safe) — never the secret key.
# Apply `supabase/migrations/` first (Supabase CLI, or paste into the SQL Editor).
NEXT_PUBLIC_SUPABASE_URL=https://aryrakxdxxvkhisdsnba.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_WOTm7ejlZ-M9jLxD_T2d_A_VMsXxlRE
